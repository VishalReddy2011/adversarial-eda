Independent t-test

Use Welch's independent t-test when comparing one numeric outcome across exactly two groups.

Assumptions:
- grouping column has exactly two observed groups
- each group has enough observations
- observations are independent

Report t statistic, p_value, n, group sizes, and Cohen's d. Reject tests with too few observations per group.
