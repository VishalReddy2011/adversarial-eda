Spearman correlation

Use when both variables are numeric or ordinal and the relationship may be monotonic rather than linear.

Assumptions:
- at least 3 paired observations
- both variables have non-zero variance after ranking
- observations are independent

Prefer this over Pearson when outliers or non-normal numeric variables make a rank-based test more appropriate.
