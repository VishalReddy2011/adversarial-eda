Pearson correlation

Use when both variables are numeric and the expected relationship is approximately linear.

Assumptions:
- at least 3 paired observations
- both variables have non-zero variance
- relationship is roughly linear
- extreme outliers are not driving the result

Report r, p_value, and n. A statistically significant p_value with a very small absolute r is weak evidence.
